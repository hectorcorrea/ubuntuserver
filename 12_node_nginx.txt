# create /etc/nginx/conf.d/nodesite.conf with the following

server {
  listen       80;
  server_name  nodesony;

  #charset koi8-r;
  #access_log  /var/log/nginx/log/host.access.log  main;

  location / {
    #proxy_set_header Host $host;
    #proxy_set_header X-Forwarded-For $remote_addr;
    proxy_pass http://127.0.0.1:3000/;
    break;
  }
}


# this file will be automatically loaded by nginx as long as the default
# config under /etc/nginx/nginx.conf has a line like this:
include /etc/nginx/conf.d/*.conf;


# reload nginx 
sudo service nginx reload


# stop/start nginx 
sudo service nginx stop
sudo service nginx start